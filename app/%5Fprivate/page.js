/* 
    start with %5F prefix in folder name
    can access with directly with url. in broweser window 
*/
export default function PrivatePage() {
    return (
        <div>
            <h1>Private Page with %5F prefix</h1>
        </div>
    );
}