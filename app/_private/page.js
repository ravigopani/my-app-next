/* 
    start with _ prefix in folder name
    can't be access with directly with url. in broweser window 
*/
export default function PrivatePage() {
    return (
        <div>
            <h1>Private Page with _ prefix</h1>
        </div>
    );
}